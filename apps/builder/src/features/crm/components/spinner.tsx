import { Loader2Icon } from "lucide-react"

/** The one inline loading mark the CRM 360 surfaces share. */
export function Spinner() {
  return <Loader2Icon className="size-4 animate-spin text-muted-foreground" />
}
